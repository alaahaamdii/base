## Notes

**Standalone Components**

`To generate a standalone component : ng generate component hello --standalone`


**Babel** 

`They are used to transpile modern JavaScript (ES6+) into backward-compatible code.` 

`These specific packages handle tasks like async transformation, runtime helpers, and environment presets.`

**Hoisting**

`I think we don't need it here`

**Create Object or Class**

`Like with function, arrow vs regular function`

`Is there destructuring ?`

`Optional params`

`Rest Vs Spread`

`Get/Set with _variable`

`Create Promise with arrow functions if possble and the use of async await`

`This in angular context`

`Types/Interface`

`Create well the function with the input/output and all`

`Decorator`

`Pick/Partial/ReadOnly`

`Record`

`Types/Interface`

`Eslint`

`Change Detection`

**CLI**

la CLI appelle le compilateur TypeScript. Le compilateur génère des
fichiers JavaScript. La CLI va alors les assembler (bundle) et ajouter les éléments script nécessaires au fichier index.html (en utilisant pour cela Vite)

**Bubbling up**

`Event preventDefault / stopPropagation`

**Test**

**Http**

Mais la plupart des développeurs Angular utilisent un service fourni par Angular : HttpClient.
Si tu veux l’utiliser, il te faudra utiliser les classes et fonctions du package @angular/common/http.

Context

**Routing**

Resolver

**Forms**

pilotée par le template/code

**Rxjs/Ngrx**

**Component/Directive**
